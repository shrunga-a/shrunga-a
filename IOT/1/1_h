#Display the number of lines and number of words in a each line

fl = open(input("Enter the file name:"))
count = 0
for i in fl:
	lst = i.split()
	count +=1
	print("The number of words in the line",count,"is",len(lst))
